# Production Environment Variables
# These should be set as container environment variables, not in this file

# AWS RDS Database Configuration  
DATABASE_URL=${DATABASE_URL}

# AWS Configuration (from IAM role or environment)
AWS_REGION=${AWS_REGION}

# Cognito Configuration
COGNITO_USER_POOL_ID=${COGNITO_USER_POOL_ID}
COGNITO_CLIENT_ID=${COGNITO_CLIENT_ID}
COGNITO_REGION=${COGNITO_REGION}

# API Configuration
API_V1_PREFIX=/api/v1
SECRET_KEY=${SECRET_KEY}
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# External APIs (for plant identification)
PLANT_ID_API_KEY=${PLANT_ID_API_KEY}
PLANT_NET_API_KEY=${PLANT_NET_API_KEY}

# Application Settings
DEBUG=False
ALLOWED_HOSTS=${ALLOWED_HOSTS}
CORS_ORIGINS=${CORS_ORIGINS}

# S3 Configuration
S3_BUCKET_NAME=${S3_BUCKET_NAME}